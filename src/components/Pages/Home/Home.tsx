import React, { } from 'react';
import Converter from '../../Converter/Converter';

import './Home.scss';

export const Home = () => {

    return (
        <div className='home'>
            <Converter />
        </div>
    );
};